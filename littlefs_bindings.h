/* Bindings for the LittleFS component, generated separately from the ESP-IDF
 * ones (see `bindings_module` in Cargo.toml) so the two do not duplicate every
 * IDF header they share. */
#include "esp_littlefs.h"
